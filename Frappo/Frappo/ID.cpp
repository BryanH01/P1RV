#include "ID.h"

using namespace std;

ID::ID() {
	string a;
	vector<double> b;
	personne = a;
	score = b;
}

void ID::setPersonne(string a) {
	personne = a;
}

void ID::setScore(vector<double> b) {
	score = b;
}

double ID::distance(ID id) {
	
}